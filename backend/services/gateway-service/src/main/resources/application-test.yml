server:
  port: 8080

spring:
  application:
    name: gateway-service-test
  main:
    web-application-type: reactive

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: '*'
            allowedMethods: '*'
            allowedHeaders: '*'
      routes:
        - id: appointment-service-test
          uri: lb://appointment-service-test
          predicates:
            - Path=/api/appointments/**

        - id: auth-service-test
          uri: lb://auth-service-test
          predicates:
            - Path=/api/auth/**

        - id: dentist-service-test
          uri: lb://dentist-service-test
          predicates:
            - Path=/api/dentists/**

        - id: logging-service-test
          uri: lb://logging-service-test
          predicates:
            - Path=/api/logs/**

        - id: monitoring-service-test
          uri: lb://monitoring-service-test
          predicates:
            - Path=/api/metrics/**

        - id: notification-service-test
          uri: lb://notification-service-test
          predicates:
            - Path=/api/notifications/**

        - id: patient-service-test
          uri: lb://patient-service-test
          predicates:
            - Path=/api/patients/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: false

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
